name: Build Stackblaze Workspace image

on:
  workflow_dispatch:
  push:
    branches: [main, ci/build-stackblaze-workspace]
    paths:
      - 'services/stackblaze-workspace/**'
      - '.github/workflows/build-stackblaze-workspace.yaml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: [self-hosted, kubero]
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push workspace image
        uses: docker/build-push-action@v6
        with:
          context: services/stackblaze-workspace
          file: services/stackblaze-workspace/Dockerfile
          push: true
          tags: |
            ghcr.io/stackblaze/stackblaze-workspace:latest
            ghcr.io/stackblaze/stackblaze-workspace:${{ github.sha }}
